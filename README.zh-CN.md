# graph

[English](README.md) · **中文**

把一篇内容——文章、研究报告、论文、新闻汇总、视频文字稿或数据集——变成单文件交互式 HTML 页面，*用可视化把它讲明白*。

页面是讲解员，不是文档：手写 SVG 图表承载论证，简短文字点出结论，交互（悬停读数、日夜主题）服务想深入看的读者。

## 你会得到什么

- **单个自包含 HTML 文件** —— 所有 CSS、JS、SVG、数据全部内联。双击就能打开，发给朋友，拷进 U 盘都行。无框架、无 CDN、读者不需要任何构建步骤。
- **日夜双主题** —— 纸感（暖灰 + 朱橙）与墨感（近黑 + 金），角落一键切换。
- **手写 SVG 图表** —— 不依赖任何图表库。视觉语言由 skill 的设计 token 定义，而不是 ECharts 或 d3。
- **只有两种交互** —— 悬停读数（最近数据点的精确值）与主题切换。克制就是美学。
- **诚实为本** —— 页面上的每个数字都溯源到输入或引用来源；对数轴、抽样、取整、假设全部在图注中声明；来源行是必须的。

## 使用方法

graph 是 Agent Skills 风格的 skill——`SKILL.md` 是入口。把它装进你的 agent 的 skill 目录（Claude Code、Codex、Hermes，或任何支持 skill 的 agent），然后让它可视化一段内容：

```
把这篇文章做成页面：<贴文章>
可视化这份数据：<数据集>
把这篇论文做成 HTML 讲解页：<arxiv 链接或 PDF 文本>
```

agent 读取 `SKILL.md`，按七步流水线执行：理解 → 提炼 → 叙事 → 选图元 → 组装 → 构建+验证 → 留档。

## 仓库结构

```
SKILL.md          入口：流水线、规则、分工
scripts/          build.py（多文件 → 单 HTML）、validate.py（自检）
references/       设计 token、叙事结构、交互、图元库
workflows/        按输入类型的处理流（文章 / 论文 / 数据 / 新闻 / 文字稿）
```

## 设计哲学

**风格是法律，执行是自由。** skill 定死风格系统（颜色、字体、间距、动效、诚实规则、技术坑）和信息（输入意味着什么、什么绝对不能发生）——但从不规定具体画法。组合的 agent 在这些边界内有完全的自由，这正是每一页都鲜活而非模板化的原因。

skill 从真实运行中进化：真实页面的反馈回流进 references，知识随使用增长。

## 协议

MIT © 2026 ruifengchen
